---
tag: [ vce ]
---

# To init a submodule (add the entry to .git/config)
git submodule init <submodule>

# To deinit a submodule (remove the entry in .git/config, and clear the directory if exists)
git submodule deinit <submodule>

# To remove the git directory of the submodule
rm -rf .git/modules/<submodule>

# To update submodule (update the working directory)
git submodule update [--remote] [--recursive]
