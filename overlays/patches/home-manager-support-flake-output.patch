diff --git a/home-manager/home-manager b/home-manager/home-manager
index 5bf796b..ef00504 100644
--- a/home-manager/home-manager
+++ b/home-manager/home-manager
@@ -190,6 +190,11 @@ function setFlakeAttribute() {
         fi
     fi
 
+    if [[ -n "$FLAKE_OUTPUT_ARG" ]]; then
+        export FLAKE_CONFIG_URI="$FLAKE_OUTPUT_ARG"
+        return
+    fi
+
     if [[ -n "$FLAKE_ARG" ]]; then
         local flake="${FLAKE_ARG%#*}"
         case $FLAKE_ARG in
@@ -955,6 +960,7 @@ PASSTHROUGH_OPTS=()
 COMMAND=""
 COMMAND_ARGS=()
 FLAKE_ARG=""
+FLAKE_OUTPUT_ARG=""
 
 while [[ $# -gt 0 ]]; do
     opt="$1"
@@ -988,6 +994,11 @@ while [[ $# -gt 0 ]]; do
             FLAKE_ARG="$1"
             shift
             ;;
+        --flake-output)
+            [[ -v 1 && $1 != -* ]] || errMissingOptArg "$opt"
+            FLAKE_OUTPUT_ARG="$1"
+            shift
+            ;;
         --recreate-lock-file|--no-update-lock-file|--no-write-lock-file|--no-registries|--commit-lock-file|--refresh)
             PASSTHROUGH_OPTS+=("$opt")
             ;;
