# Add new dependencies.
# default: dependencies
# -D: devDependencies
# -g: install packages globally
pnpm add <pkg[@tag]>..

# Remove packages
pnpm rm|un|uninstall|remove

# Install project dependencies according to `package.json` and `pnpm-lock.yaml`.
# --offline: Install offline from the store only
# --frozen-lockfile: pnpm-lock.yaml is not updated
pnpm i 

# Remove unnecessary packages.
pnpm prune
